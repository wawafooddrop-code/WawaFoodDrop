<div class="bg-surface-container-lowest p-5 rounded-card border border-outline-variant/30 flex flex-col gap-4 hover:shadow-lg transition-all">
      <div class="flex justify-between items-start gap-2">
        <div class="flex flex-col gap-1">
          <p class="text-[10px] text-on-surface-variant font-bold uppercase tracking-widest">Order ID</p>
          <p class="font-mono font-extrabold text-sm text-primary">#${orderId.substring(0, 8)}</p>
        </div>
        <div class="text-right">
          <p class="text-2xl font-black font-headline text-secondary">?${((order.subtotal||0) + (order.deliveryFee||0)).toLocaleString('en-PH', { minimumFractionDigits: 2 })}</p>
          <p class="text-[10px] text-on-surface-variant font-bold uppercase tracking-widest">Total</p>
        </div>
      </div>
      <div class="bg-surface-container-low p-3 rounded-lg flex flex-col gap-1">
        <div class="flex justify-between text-sm">
          <span class="text-on-surface-variant">Order Cost</span>
          <span class="font-semibold">?${(order.subtotal||0).toLocaleString('en-PH', { minimumFractionDigits: 2 })}</span>
        </div>
        <div class="flex justify-between text-sm">
          <span class="text-on-surface-variant">Delivery Fee</span>
          <span class="font-semibold">?${(order.deliveryFee||0).toLocaleString('en-PH', { minimumFractionDigits: 2 })}</span>
        </div>
        <div class="border-t border-outline-variant/50 mt-1 pt-1 flex justify-between text-sm font-bold">
          <span class="text-on-surface-variant">Your Earnings</span>
          <span class="text-primary">?${calcRiderCommission((order.subtotal||0)).toLocaleString('en-PH', { minimumFractionDigits: 2 })}</span>
        </div>
      </div>
      ${badge}
      <div>
        <h4 class="font-bold text-lg">${restName}</h4>
        <div class="flex items-center gap-1 mt-1 text-on-surface-variant font-medium text-sm">
          <span class="material-symbols-outlined text-sm">location_on</span>
          <span>${order.deliveryAddress || order.customerAddress || '—'}</span>
        </div>
        <div class="flex items-center gap-1 mt-1 text-on-surface-variant font-medium text-sm">
        
